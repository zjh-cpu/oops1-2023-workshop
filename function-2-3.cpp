#include <iostream>
using namespace std;
int palindrome_sum(int integers[], int length){
    for(int i=0,i<4,i++){
        for(int j=0,j<4,j++)
        
        if(i+j=0)
        palindrome_sum=palindrome_sum+integers[]
        return 0;
        else 
        return 1;
    }
}